{
  "name": "bottrace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verifiable execution trails for autonomous AI agents on BOT Chain.",
  "scripts": {
    "dev": "node apps/web/server.mjs",
    "build": "npm run check",
    "demo": "node apps/agent/demo.mjs",
    "deploy:registry": "node scripts/deploy-registry.mjs",
    "submit:demo": "node scripts/submit-demo-receipt.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check apps/web/public/app.js && node --check apps/web/server.mjs && node --check apps/agent/demo.mjs && node --check packages/core/receipt.mjs && node --check packages/botchain/blackboxClient.mjs && node --check scripts/prepare-submit.mjs && node --check scripts/deploy-registry.mjs && node --check scripts/submit-demo-receipt.mjs"
  },
  "keywords": [
    "bot-chain",
    "evm",
    "ai-agents",
    "audit",
    "receipts"
  ],
  "license": "MIT",
  "devDependencies": {
    "ethers": "^6.17.0",
    "solc": "^0.8.35"
  }
}
